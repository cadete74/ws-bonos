services:
  ws-bonos:
    build: .
    container_name: ws-bonos
    restart: unless-stopped
    environment:
      - FETCH_INTERVAL_SECONDS=5
    ports:
      - "8000:8000"     # ← publica el puerto en el host
    networks:
      - shared-net

networks:
  shared-net:
    external: true
